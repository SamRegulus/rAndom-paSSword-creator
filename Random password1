import random
import string
password_length = int(input("Enter the desired length of password: "))
charactres = string.ascii_letters + string.digits + "!@#$&+="
password = ''.join(random.sample(charactres, password_length))
print(password)
